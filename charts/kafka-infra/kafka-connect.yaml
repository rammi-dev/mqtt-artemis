apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnect
metadata:
  name: telemetry-connect
  namespace: kafka
  annotations:
    strimzi.io/use-connector-resources: "true"
spec:
  version: 3.7.0
  replicas: 1
  bootstrapServers: telemetry-cluster-kafka-brokers.default.svc.cluster.local:9092
  image: my-kafka-connect:v5
  
  # Top-level fields required by Strimzi v1beta2 (recent)
  groupId: connect-cluster
  offsetStorageTopic: connect-cluster-offsets
  configStorageTopic: connect-cluster-configs
  statusStorageTopic: connect-cluster-status
  
  config:
    # Key/Value Converters remain here
    key.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: false
    value.converter.schemas.enable: false
    config.storage.replication.factor: 1
    offset.storage.replication.factor: 1
    status.storage.replication.factor: 1
