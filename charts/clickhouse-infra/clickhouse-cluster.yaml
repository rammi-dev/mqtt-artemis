apiVersion: clickhouse.altinity.com/v1
kind: ClickHouseInstallation
metadata:
  name: telemetry-db
  namespace: clickhouse
spec:
  templates:
    podTemplates:
      - name: simple-pod
        spec:
          containers:
            - name: clickhouse
              image: clickhouse/clickhouse-server:24.3
              resources:
                requests:
                  memory: "512Mi"
                  cpu: "500m"
                limits:
                  memory: "1Gi"
                  cpu: "1000m"
  configuration:
    users:
      admin/password: password
      admin/networks/ip: "0.0.0.0/0"
      admin/profile: default
    clusters:
      - name: main
        layout:
          shardsCount: 1
          replicasCount: 1
        templates:
          podTemplate: simple-pod
